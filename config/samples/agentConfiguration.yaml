apiVersion: deployments.plural.sh/v1alpha1
kind: AgentConfiguration
metadata:
  labels:
    app.kubernetes.io/part-of: deployment-operator
    app.kubernetes.io/created-by: deployment-operator
  name: default
spec:
  clusterPingInterval: "2m"
  runtimeServicesPingInterval: "5m"
  stackPollInterval: "0s"
  pipelineGateInterval: "0s"
  vulnerabilityReportUploadInterval: "10m"
  maxConcurrentReconciles: 5

