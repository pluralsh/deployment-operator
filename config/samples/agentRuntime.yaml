apiVersion: deployments.plural.sh/v1alpha1
kind: AgentRuntime
metadata:
  name: test-claude-dind
  namespace: claude
spec:
  type: "CLAUDE"
  targetNamespace: "claude"
  config:
    claude:
      apiKeySecretRef:
        name: claude-api-key
        key: apiKey
  template:
    spec:
      containers:
      - name: default
        image: ghcr.io/pluralsh/agent-harness:sha-c473491-claude-1.0.128
        args:
          - --v=3





