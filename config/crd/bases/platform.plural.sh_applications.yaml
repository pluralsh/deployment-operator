---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: applications.platform.plural.sh
spec:
  group: platform.plural.sh
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    shortNames:
    - app
    - apps
    singular: application
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is a definition of Application resource.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              namespace:
                description: Namespace to sync into.
                type: string
              pluralId:
                description: PluralId is an ID of deployment from Plural.
                type: string
              ref:
                description: Ref to fetch from the repository.
                type: string
              repoUrl:
                description: RepoUrl is a URL of repository to sync.
                type: string
              subfolder:
                description: Subfolder from the repository to work with.
                type: string
            required:
            - namespace
            - pluralId
            - ref
            - repoUrl
            - subfolder
            type: object
          status:
            properties:
              ref:
                description: Ref shows current ref its synced to.
                type: string
              resources:
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    status:
                      description: ApplicationResourceStatus represents current status
                        of application resource.
                      enum:
                      - Pending
                      - Failed
                      - Succeeded
                      type: string
                    synced:
                      type: boolean
                  required:
                  - apiVersion
                  - kind
                  - name
                  - namespace
                  - status
                  - synced
                  type: object
                type: array
            required:
            - ref
            - resources
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
